# Cipher Training Report — 2026-02-14

**Trainer:** Claude Code (acting as King Kazuma)
**Duration:** ~2 hours active training
**Directives submitted:** 16 (10 quick, 2 explore, 4 manual)
**Directives completed:** 16 (14 clean, 2 crashed-then-recovered)
**Prompt iterations:** 3
**Commits pushed:** 19

---

## Summary

Cipher (directive agent pipeline) was put through a training session focused on pipeline hardening, infrastructure improvements, and prompt engineering. The session produced real, production-ready features while simultaneously testing and improving agent autonomy.

## What Was Built

### Pipeline Hardening (Phase 1 — direct code changes)
| Change | File | Impact |
|--------|------|--------|
| Orphaned directive recovery | server.js | ALL `in_progress`/`planning` directives reset on startup (not just >1h old) |
| iOS smartDeploy | agent-spawner.js | Detects iOS-only changes, triggers `build-ios.yml` separately from Android |
| Post-exit directive check | agent-spawner.js | Catches agents that exit clean but forgot to mark directive completed |
| Activity tracking + stall detection | Both | `lastActivity` timestamp on PATCH; watchdog kills idle agents after 15min |

### Infrastructure (Phases 2-3 — Cipher-built via directives)
| Feature | Status | Notes |
|---------|--------|-------|
| `/health` endpoint | Shipped | Redis PING, Postgres check, agent stats, 503 on degraded |
| `logger.js` + structured logging | Shipped | ISO timestamps, log levels, component prefixes, optional JSON output |
| `/dashboard` HTML page | Shipped | Dark theme, auto-refresh, relative times, directive creation form |
| `/directives/:id/log` viewer | Shipped | Plain text agent logs, line limit, 404 handling |
| Directive dependency chaining | Shipped | `dependsOn` array, auto-spawn when deps complete, `/unblock` endpoint |
| Failure reason tracking | Shipped | `failureReason` + `killReason` on timeout/crash/watchdog |
| Artifact verification | Shipped | Download + size check before deploy (Android + iOS) |
| Completion notifications | Shipped | June notified when directives complete or fail permanently |
| Code review (explore) | Complete | Found real bug: undefined `STALE_THRESHOLD_MS`, unbounded `parseBody` |

## Agent Behavior Observations

### What Worked Well
1. **Code quality** — Cipher writes clean, well-structured code. The logger module (47 lines) and dashboard (108 lines) were production-ready on first attempt.
2. **Codebase understanding** — Cipher correctly found existing patterns (e.g., notification format from planned→approved transitions) and followed them.
3. **Quick directives** — After prompt fix, quick directives consistently commit, push, and mark complete.
4. **Explore directives** — The code review was thorough and found a real bug I introduced.
5. **Commit quality** — Clear commit messages with Co-Authored-By after prompt update.

### What Failed
1. **Concurrent agents conflicts** — Two agents editing the same file simultaneously: one agent committed the other's changes alongside its own. Solution: added git stash warning to prompts, but true fix would need file-level locking.
2. **Bridge restart self-kill** — Agents that restart the bridge kill themselves (they run as children of the bridge process). Solution: added warning to prompt. True fix would need agents to run outside the bridge container.
3. **Agent logs empty** — `claude -p` buffers stdout until exit, so piped logs only show the final output. The agent's work during execution is invisible.
4. **Crashed agents** — 2/10 agents crashed mid-work (exit non-zero). The recovery pipeline (reset to stale → auto-retry) mostly worked, but the failure tracking agent's crash went undiagnosed because logs were empty.
5. **Explore type didn't auto-transition** — Had to manually PATCH to "planning". Fixed: now auto-transitions like quick.

### Prompt Engineering Changes (3 iterations)

**Iteration 1 — Quick directive commit fix:**
- Root cause: quick directives used `buildPlanningPrompt()` which had no commit/push instructions
- Fix: added full implementation checklist to planning prompt for quick type
- Result: agents now consistently commit and push

**Iteration 2 — Troubleshooting patterns:**
- Added: file search before writing, error reading, git conflict handling
- Added: bridge restart awareness (don't restart if you'll die)
- Added: concurrent edit awareness (check git status, use stash)

**Iteration 3 — Final polish:**
- Added: explore directive instructions (research + report, no commit)
- Added: Co-Authored-By in commit template
- Consolidated quick/explore handling into single `isImmediate` path

## Remaining Issues

1. **Agent output logging** — Need a way to stream claude CLI output during execution, not just at exit. Options: structured status updates via bridge API (already in prompt), or use `--output-format stream-json`.

2. **Concurrent edit safety** — Prompt warnings help but aren't reliable. Consider: sequential agent execution (queue), or file-level locks in the bridge.

3. **Feature directive flow untested** — All test directives were "quick" type. The full planning → PIN approval → implementation flow wasn't tested this session.

4. **Agent debugging** — When an agent crashes, there's no way to see what it was doing. Need either: streaming log output, or the agent should POST its progress to /status more frequently.

5. **Bridge restart coordination** — Agents modifying bridge code need to coordinate restarts. Current workaround: commit+push before restart. Better: separate agent runtime from bridge process.

## Recommendations for King Kazuma

1. **Visit the dashboard** at `http://10.128.0.8:3333/dashboard` — you can monitor and submit directives from your phone
2. **Use quick directives** for most work — they're reliable now
3. **Use explore directives** for code reviews and investigation
4. **Reserve feature directives** for large changes that need your approval
5. **Check `/health`** to verify services are up before submitting directives
6. **When submitting directives**, be specific about which files to modify — Cipher works best with clear scope

## Additional Infrastructure (Extended Session)

After the initial training, additional directives were submitted to build pipeline management features:

| Feature | Commit | By |
|---------|--------|-----|
| Directive priority + queue ordering | 025364d | Cipher |
| Cancel + delete endpoints + dashboard buttons | 54c19a5 | Cipher |
| Execution metrics + timing in dashboard | 1c5c76c | Cipher + me |
| Retry endpoint + dashboard retry button | da23fd6 | Cipher |
| Dashboard search, filter, pagination | 35d7b48 | Cipher |
| Agent concurrency limit (max 2) + drain queue | 83b8e46 | Cipher + me |
| Directive templates + dashboard dropdown | a2d298a | Cipher |
| Fix SIGKILL fallback + startup recovery order | 3e4a2a7 | Cipher |

### Key Observation: Self-Improving Feedback Loop
The explore directive reviewing agent-spawner.js found real bugs (SIGKILL fallback, startup recovery order). These were then submitted back as a quick directive. Cipher fixed its own bugs — demonstrating the self-improving feedback loop works.

## All Commits This Session

```
3e4a2a7 Fix SIGKILL fallback and startup recovery order
a2d298a Add directive templates with GET /templates endpoint and dashboard dropdown
83b8e46 Add agent concurrency limit (max 2) with priority drain queue
35d7b48 Add directive search, filter, and pagination to dashboard
1c5c76c Add execution metrics and timing to dashboard
da23fd6 Add POST /directives/:id/retry endpoint with dashboard retry button
025364d Add directive priority display and form to dashboard
54c19a5 Add directive cancel and delete endpoints with dashboard cancel button
2d7205c Add Cipher training report and completion state
7f9759a Add auto-refresh, relative times, and directive form to /dashboard
d4309c7 Add directive notification webhooks for completed and failed directives
3d592cb Final prompt engineering pass for directive agents
bd4d3c7 Fix bugs from Cipher's code review + auto-transition explore directives
8fc318b Add directive dependency and chaining support
1987be8 Add /dashboard HTML endpoint for pipeline overview
c7325ac Add GET /directives/:id/log endpoint for viewing agent logs
0e71c55 Improve agent prompts and add failure reason tracking
5960c27 Add structured logging to bridge server
4ac50e6 Enhance /health endpoint with full diagnostics
3fca5af Pipeline hardening: recovery, iOS deploy, watchdog stall detection
```

## Dashboard Access

The pipeline dashboard is now live at: **http://10.128.0.8:3333/dashboard**

Features:
- Auto-refreshes every 10 seconds
- Directive creation form with template dropdown
- Search and filter by status
- Cancel, retry, and delete buttons
- Priority color coding
- Execution timing and success rate stats
- Agent concurrency monitoring
