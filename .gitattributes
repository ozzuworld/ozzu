# git-crypt: encrypt secrets at rest in the repo

# OpenVPN certificates — removed from git-crypt (Docker bind mounts need plaintext)

# Nginx / Certbot credentials
backend/nginx/cloudflare.ini filter=git-crypt diff=git-crypt

# Environment variables
# backend/.env — removed from git-crypt (causes docker-compose failures, config in docker-compose.yml instead)
