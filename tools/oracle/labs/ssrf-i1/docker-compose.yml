services:
  web:
    build: .
    container_name: ozzulab-ssrf-i1-web
    restart: unless-stopped
    networks: { labnet: { ipv4_address: 10.10.31.20 } }
networks:
  labnet:
    ipam: { config: [ { subnet: 10.10.31.0/24 } ] }
